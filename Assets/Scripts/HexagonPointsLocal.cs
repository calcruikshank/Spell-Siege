using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HexagonPointsLocal : MonoBehaviour
{
    Vector3 topRight = new Vector3(0.5f, 0.15f, 0.2875f);
    Vector3 topLeft = new Vector3(-0.5f, 0.15f, 0.2875f);
    Vector3 top = new Vector3(0, 0.15f, .575f);
    Vector3 bottomRight = new Vector3(0.5f, 0.15f, -0.2875f);
    Vector3 bottomLeft  = new Vector3(-0.5f, 0.15f, -0.2875f);
    Vector3 bottom  = new Vector3(0, 0.15f, -.575f);
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
